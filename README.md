# Computer_vision_project
The project solution is not appositely not based on deep learning models, in order to make experience about features that are present inside an image. The algorithm 
is based on a series of cascade classifier which output is processed through a SVM.
